{
  "version": "0.2.0",
  "configurations": [
    {
      // Need to run Edge with --remote-debugging-port=2015
      "name": "Edge",
      "type": "edge",
      "request": "attach",
      "port": 2015,
      "url": "http://localhost:4200/#/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
