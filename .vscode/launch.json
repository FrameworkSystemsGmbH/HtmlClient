{
  "version": "0.2.0",
  "configurations": [
    {
      // Need to run Chrome with --remote-debugging-port=9222
      "name": "Chrome",
      "type": "chrome",
      "request": "attach",
      "port": 9222,
      "url": "http://localhost:4200/#/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
